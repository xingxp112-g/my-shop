# 标签二级管理功能 SPEC

> 访谈日期：2026-03-11
> 分支：feat/tag-hierarchy
> 状态：待开发

---

## 1. 功能概述

为标签体系增加两级结构：

- **一级标签**：现有标签保持不变，可选择是否在其下创建二级子标签
- **二级标签**：挂在某个一级标签下，可创建/编辑/删除

---

## 2. 数据库变更

### tag 表新增字段

| 字段 | 类型 | 说明 |
|------|------|------|
| parent_id | INT FK NULL | 指向 tag.id；NULL 表示一级标签，有值表示二级标签 |

> 不新增表，一律使用 `parent_id` 自关联。
> 一级标签：`parent_id IS NULL`
> 二级标签：`parent_id = <一级标签 id>`

---

## 3. 业务规则

### 3.1 商品关联标签

- **一级标签有二级子标签时**：商品只能关联该一级标签下的二级标签，**不得直接关联该一级标签**
- **一级标签无二级子标签时**：商品可直接关联该一级标签（兼容旧逻辑）
- **一件商品可以关联多个二级标签**，跨不同一级组也可以

### 3.2 删除规则

**删除一级标签：**
- 若该标签下存在任何二级子标签 → **拒绝删除**，提示「请先删除该标签下的所有子标签」

**删除二级标签：**
- 若有商品关联该二级标签 → 弹出确认框，展示「该标签下有 X 件商品将失去此标签，确认删除？」
- 用户确认 → 删除标签并**自动解除**商品与该标签的关联
- 若无商品关联 → 直接删除（无需确认）

### 3.3 存量数据兼容

现有商品已关联了一级标签，若之后该一级标签下新增了二级子标签：
- **不强制迁移**，已关联一级标签的商品数据保持不变
- 前台展示时，该类商品归入该一级标签的「全部」中显示
- 运营人员可随时在后台手动编辑商品，将标签改为对应的二级标签

### 3.4 商品表单约束

- 后台编辑/新增商品时，若选择的一级标签已有二级子标签，**必须同时勾选至少一个二级标签**，否则不允许保存

---

## 4. 后端 API 变更

### 4.1 GET /tags

支持按层级过滤：

| 查询参数 | 说明 |
|---------|------|
| （无参数） | 返回完整树形结构，一级标签含 `children` 数组 |
| `level=1` | 只返回一级标签（不含 children） |
| `parent_id=<id>` | 返回某一级标签下的所有二级标签 |

返回结构示例（无参数时）：
```json
[
  {
    "id": 1,
    "name": "护肤",
    "parent_id": null,
    "children": [
      {"id": 5, "name": "精华液", "parent_id": 1},
      {"id": 6, "name": "面霜", "parent_id": 1}
    ]
  },
  {
    "id": 2,
    "name": "彩妆",
    "parent_id": null,
    "children": []
  }
]
```

### 4.2 POST /tags

新增 `parent_id` 字段（可选，默认 null 为一级标签）：
```json
{ "name": "精华液", "parent_id": 1 }
```

### 4.3 PUT /tags/{id}

仅支持修改 `name`，**不允许修改 `parent_id`**（不支持层级迁移）。

### 4.4 DELETE /tags/{id}

- 删除一级标签时，若存在子标签 → 返回 400 + 提示信息
- 删除二级标签时，若有关联商品 → 返回关联商品数供前端弹确认框，用户确认后再调用删除

**新增辅助接口：**
- `GET /tags/{id}/product-count` → 返回关联该标签的商品数量（供删除前确认弹框使用）

---

## 5. 后台管理 UI 变更

### 5.1 tags.html（标签管理页）

**表格内展开子标签：**

```
| ID | 标签名 | 子标签数 | 操作                            |
|----|------|--------|----------------------------------|
|  1 | 护肤   | 3      | [展开▼] [编辑] [+子标签] [删除]   |
|    | └ 精华液 |        | [编辑] [删除]                   |
|    | └ 面霜   |        | [编辑] [删除]                   |
|    | └ 乳液   |        | [编辑] [删除]                   |
|  2 | 彩妆   | 0      | [编辑] [删除]                   |
```

- 一级标签新增「子标签数」列
- 有子标签的一级标签显示「展开▼/折叠▲」，点击展开/收起子标签行
- 子标签行缩进显示，仅有「编辑」「删除」操作
- 一级标签操作列额外含「+子标签」快捷入口

**删除 UI 反馈：**
- 删除有子标签的一级标签 → Toast 提示「请先删除子标签」，不弹确认框
- 删除有关联商品的二级标签 → 弹确认框「该标签下有 X 件商品将失去此标签，确认删除？」
- 删除无关联商品的二级标签 → 直接删除

### 5.2 products-form.html（商品新增/编辑页）

**标签选择区域改为分组展示：**

```
标签：
  护肤（有子标签，作为分组标题，不可直接勾选）
   ☐ 精华液   ☐ 面霜   ☐ 乳液
  彩妆
   ☒ 口红    ☐ 眼影
  身体护理（无子标签，直接可勾选）
   ☒ 护手霜
```

- 有子标签的一级标签名称作为**分组标题**，不可勾选
- 无子标签的一级标签直接显示为可勾选项
- 支持勾选多个二级标签（跨组也可以）
- 保存时校验：若勾选了有子标签的分组，必须至少勾选一个该分组下的二级标签

---

## 6. 前台 H5 变更

### 6.1 index.html（商品列表页）

**标签筛选区（第一行）：**

```
[所有商品]  [护肤 ▼]  [彩妆]  [身体护理]
```

- 有子标签的一级标签显示展开指示（如 ▼）
- 无子标签的一级标签外观与现在一致

**点击有子标签的一级标签后，内联展开第二行：**

```
[所有商品]  [护肤 ▲]  [彩妆]  [身体护理]
            [全部] [精华液] [面霜] [乳液]
```

- 第二行紧跟在标签栏下方内联展开，不弹出浮层
- 默认选中「全部」
- 同时只展开一个一级标签的二级行，点击其他一级标签时收起当前展开行

**点击无子标签的一级标签：**
- 行为与现在一致，直接按该标签筛选，不展开任何二级行

### 6.2 筛选逻辑

| 用户操作 | 筛选条件 |
|---------|---------|
| 「所有商品」 | 不过滤标签 |
| 有子标签的一级标签「全部」 | `tag_id IN (所有子标签 id) OR tag_id = 一级标签 id`（兼容存量数据） |
| 某二级标签 | `tag_id = 二级标签 id` |
| 无子标签的一级标签 | `tag_id = 一级标签 id` |

---

## 7. 不在本次范围内

- 三级及更深层级标签
- 标签排序（拖拽/权重）
- 批量迁移存量商品标签
- 标签统计数据展示

---

## 8. 文件变更清单

| 文件 | 变更说明 |
|------|---------|
| `backend/sql/init.sql` | tag 表新增 `parent_id` 字段 |
| `backend/app/models/tag.py` | ORM 新增 `parent_id`，添加自关联 relationship |
| `backend/app/schemas/tag.py` | Schema 新增 `parent_id`、`children` 字段 |
| `backend/app/routers/tags.py` | 新增查询参数，删除校验逻辑变更 |
| `backend/app/services/tags.py` | 层级查询、删除前校验、解除关联逻辑 |
| `frontend/admin/tags.html` | 表格内展开子标签，操作按钮调整 |
| `frontend/admin/products-form.html` | 标签选择改为分组形式 |
| `frontend/h5/index.html` | 标签栏增加内联展开二级标签行 |

---

## 9. 验收标准

- [ ] 后台可在一级标签下创建、编辑、删除二级标签
- [ ] 一级标签下有子标签时，删除一级标签被拒绝并提示
- [ ] 删除有关联商品的二级标签时，弹出确认框并显示商品数；确认后标签删除且商品关联解除
- [ ] 商品表单标签选择以分组形式展示，有子标签的一级组必须选二级才能保存
- [ ] 前台点击有子标签的一级标签后内联展开二级行，默认选「全部」
- [ ] 前台点击具体二级标签，商品列表按该二级标签筛选
- [ ] 前台点击无子标签的一级标签，行为与现在一致
- [ ] 已关联一级标签的存量商品在该一级标签「全部」中正常显示
